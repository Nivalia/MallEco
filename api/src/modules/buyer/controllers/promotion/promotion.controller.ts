import { Controller, Get, Post, Body, Patch, Param, Delete } from '@nestjs/common';
import { ApiTags } from '@nestjs/swagger';

@ApiTags('促销营销')
@Controller('buyer/promotion')
export class PromotionController {
  // 促销相关接口
}
